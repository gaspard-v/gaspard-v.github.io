import { BrowserRouter } from 'react-router-dom';
import Navigator from './Navigator';
import React from 'react';

function App() {
	return (
		<BrowserRouter>
			<Navigator />
		</BrowserRouter>
	);
}

export default App;
